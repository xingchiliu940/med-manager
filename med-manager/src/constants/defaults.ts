/** 附录 B 等默认业务参数（与 PRD 对齐的可调常数） */

export const DEFAULT_STOCK_LOW_THRESHOLD = 7

/** 处方临期提醒：距失效 U 天 */
export const DEFAULT_RX_EXPIRING_DAYS = 7

/** 近效期批次预警（天），技术方案附录 B */
export const DEFAULT_NEAR_EXPIRY_DAYS = 90
